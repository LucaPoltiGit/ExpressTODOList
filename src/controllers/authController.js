const register = (req, res) => {
     res.json({ message: "Registro de usuario (pendiente de implementar)" });
}; 
const login = (req, res) => {
     res.json({ message: "Login de usuario (pendiente de implementar)" });
}; 
const profile = (req, res) => {
     res.json({ message: "Perfil de usuario (pendiente de implementar)" });
};

export {register, login, profile}